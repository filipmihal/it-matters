{
    "name": "eye-tacker",
    "version": "0.5",
    "repository": "git@github.com:filipmihal/it-matters-web.git",
    "author": "<fifomihal@gmail.com>",
    "license": "MIT",
    "scripts": {
        "eslint": "eslint --ext .jsx,.js,.tsx,.ts src/",
        "eslint:fix": "eslint --ext .jsx,.js,.tsx,.ts src/ --fix",
        "postinstall": "cd server && yarn install && yarn build && cd ../client && yarn install && yarn build && cd ../database && yarn install",
        "start": "cd server && yarn start:prod"
    },
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^4.11.0",
        "@typescript-eslint/parser": "^4.11.0",
        "eslint": "^7.16.0",
        "eslint-config-prettier": "^7.1.0",
        "eslint-plugin-prettier": "^3.3.0",
        "prettier": "^2.2.1"
    },
    "engines": {
        "node": "12.16.3"
    }
}
